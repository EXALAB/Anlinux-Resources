## This folder contain things needed to get qemu-user-static run on RHEL

### How To Use:

1. Copy qemu-debootstrap to /usr/sbin:
> sudo cp qemu-debootstrap /usr/sbin

2. Install the rpm(s):
> sudo rpm -i --nodeps *.rpm
